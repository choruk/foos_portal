<% id = name.downcase.split.join('_') %>

<div class="input-field col s6">
  <%= select_tag id, options_from_collection_for_select(User.order(:slack_user_name), :id, :slack_user_name) %>
  <label><%= name %></label>
</div>
